# LoveMarkdown
LovePrompt converts PDFs, DOCX, PPTX, and other documents into clean, AI-optimized Markdown for Claude, ChatGPT, and other LLMs. Built on top of Microsoft MarkItDown, it reduces token usage, preserves document structure, and creates prompt-ready context for RAG and AI workflows.
