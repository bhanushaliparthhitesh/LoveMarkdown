services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - '8000:8000'
  web:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    environment:
      - VITE_API_URL=http://localhost:8000
    ports:
      - '5173:5173'
    depends_on:
      - api
